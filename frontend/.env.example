# Optional: use a fixed backend origin, e.g. http://localhost:5000 or https://api.example.com
NEXT_PUBLIC_API_URL=http://localhost:5000

# Optional: override socket origin only if it differs from the API origin.
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_IP_URL=http://127.0.0.1:5000