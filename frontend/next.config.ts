import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // basePath: "/socialapp",
  output: "export",
  trailingSlash: true,
  // env: {
  //   NEXT_PUBLIC_BASE_PATH: "/socialapp",
  // },
};

export default nextConfig;
